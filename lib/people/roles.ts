/**
 * Personel — shared supporting domain (bukan Food Production, bukan HR/payroll).
 * Identitas operasional + rekening tujuan transfer. Login tetap di `users`.
 */

export const PEOPLE_MANAGE_ROLES = ['ADMIN', 'OWNER', 'SUPERVISOR', 'MASTER'] as const;
